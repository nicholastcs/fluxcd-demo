namespace: flux-system

resources:
  - ns.yaml

helmCharts:
  - releaseName: flux-operator
    name: flux-operator
    repo: oci://ghcr.io/controlplaneio-fluxcd/charts
    namespace: flux-system
    includeCRDs: true
